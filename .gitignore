
__pycache__/
.claude/
.DS_Store
*.pyc
# `python3 site_build.py`. The site SOURCE (assets, site/content/, site/data/) IS committed.
# Generated every run by fedreg.py and consumed by calendar_check.py in the same job.
# local-only test dependency (axe-core for accessibility runs); never shipped
# macOS
# Not committed: it would change on every run for a file nothing reads between runs.
# Pipeline runtime outputs (regenerated each run: review queue, drafts, approvals,
# published payloads). The pipeline CODE, prompts, config, and fixtures ARE committed.
# Python bytecode (CI and local runs; never commit)
# Session-local tool config
# The site deploy folder is a build artifact, fully reproducible from
data/fedreg_calendar.json
node_modules/
out/
review/
site/publish/
# Debug debris from manual GitHub API log fetches (a saved 403 body was accidentally
# committed 2026-08-25 and removed 2026-08-31); never commit it again.
joblog.txt
